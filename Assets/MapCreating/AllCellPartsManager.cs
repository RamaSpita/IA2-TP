using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AllCellPartsManager : ScriptableObject 
{
	/*public List<CellPart> floorProps;
	public List<CellPart> backWallProps;
	public List<CellPart> leftWallProps;
	public List<CellPart> backBeamProps;
	public List<CellPart> leftBeamProps;
	public List<CellPart> columnProps;
	public List<CellPart> vertexProps;
	
	public void AddToDataBase(CellPart cellpart)
	{
		switch(cellpart.type)
		{
			case CellPartTypes.Floor: if(!floorProps.Contains(cellpart))
			{
			floorProps.Add(cellpart); 
			}
									break;
									
			case CellPartTypes.BackWall: if(!backWallProps.Contains(cellpart))
			{
			backWallProps.Add(cellpart);
			}
									break;
									
			case CellPartTypes.LeftWall: if(!leftWallProps.Contains(cellpart))
			{
			leftWallProps.Add(cellpart);
			}
									break;			

			case CellPartTypes.BackBeam: if(!backBeamProps.Contains(cellpart))
			{
			backBeamProps.Add(cellpart);
			}
									break;	

			case CellPartTypes.LeftBeam: if(!leftBeamProps.Contains(cellpart))
			{
			leftBeamProps.Add(cellpart);
			}
									break;

			case CellPartTypes.Column: if(!columnProps.Contains(cellpart))
			{
			columnProps.Add(cellpart);
			}
									break;		

			case CellPartTypes.Vertex: if(!vertexProps.Contains(cellpart))
			{
			vertexProps.Add(cellpart);
			}
									break;						
		}
		
		cellpart.LastType = cellpart.type;
	}
	
	public void ChangeType(CellPart cellpart)
	{
		switch(cellpart.LastType)
		{
			case CellPartTypes.Floor: floorProps.Remove(cellpart);
									break;
									
			case CellPartTypes.BackWall: backWallProps.Remove(cellpart);
									break;
									
			case CellPartTypes.LeftWall: leftWallProps.Remove(cellpart);
									break;			

			case CellPartTypes.BackBeam: backBeamProps.Remove(cellpart);
									break;	

			case CellPartTypes.LeftBeam: leftBeamProps.Remove(cellpart);
									break;

			case CellPartTypes.Column: columnProps.Remove(cellpart);
									break;		

			case CellPartTypes.Vertex: vertexProps.Remove(cellpart);
									break;							
		}
		
		AddToDataBase(cellpart);
		
		
	}
	
	public void RemoveCellPart(CellPart cellpart)
	{
		switch(cellpart.type)
		{
			case CellPartTypes.Floor: if(floorProps.Contains(cellpart))
			{
			floorProps.Remove(cellpart);
			}
									break;
									
			case CellPartTypes.BackWall: if(backWallProps.Contains(cellpart))
			{
			backWallProps.Remove(cellpart);
			}
									break;
									
			case CellPartTypes.LeftWall: if(leftWallProps.Contains(cellpart))
			{
			leftWallProps.Remove(cellpart);
			}
									break;			

			case CellPartTypes.BackBeam: if(backBeamProps.Contains(cellpart))
			{
			backBeamProps.Remove(cellpart);
			}
									break;	

			case CellPartTypes.LeftBeam: if(leftBeamProps.Contains(cellpart))
			{
			leftBeamProps.Remove(cellpart);
			}
									break;

			case CellPartTypes.Column: if(columnProps.Contains(cellpart))
			{
			columnProps.Remove(cellpart);
			}
									break;		

			case CellPartTypes.Vertex: if(vertexProps.Contains(cellpart))
			{
			vertexProps.Remove(cellpart);
			}
									break;							
		}	
	}*/
	
}
